type Draft @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  createdAt: String
  updatedAt: String
  name: String
  army: String
  pointsLimit: Int
  rulesVersion: String
  dna: String
  status: String
}
